# Same-origin deploy (nginx serves UI and proxies /api): leave empty
VITE_API_BASE=

# Split deploy (UI on Vercel, API elsewhere):
# VITE_API_BASE=https://api.example.com

# Supabase (set in Vercel env vars for production builds)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
